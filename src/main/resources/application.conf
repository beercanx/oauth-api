ktor {

    deployment {
        host = "0.0.0.0"
        port = 8080
    }

    application {
        modules = [ com.sbgcore.oauth.api.ApplicationKt.main ]
    }
}

com.sbgcore.oauth.api {
    // Placeholder
}